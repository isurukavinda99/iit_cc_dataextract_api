apiVersion: v1
kind: Service
metadata:
  name: dataextract-api-service
spec:
  selector:
    app: dataextract-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8081
  type: LoadBalancer
